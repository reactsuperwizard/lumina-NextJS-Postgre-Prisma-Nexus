import { expect } from 'chai'
import 'mocha'

import dotenv from 'dotenv'

dotenv.config()

before(() => {
  // this is a setup file for all tests
})

it('should set up .dotenv', () => {
  expect(true)
})
